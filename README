This is a super lightweight endless scroll (a.k.a. endless page, pageless, without pagination,
etc.) plugin for jQuery.  It will let me trigger a callback whenever the user scrolls within a 
certain number of pixels from the bottom of the page.
